name: Daily CHN Route Generation

on:
  schedule:
    - cron: '30 16 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1. Checkout main
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # 2. Prepare working directory
      - name: Prepare working directory
        run: mkdir -p .github/output

      # 3. Install Python
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - run: pip install requests
        working-directory: .github

      # 4. Run Python script
      - name: Run generator
        run: python ../gen-chnroute.py
        working-directory: .github

      # 5. Copy output to root BEFORE checkout
      - name: Copy output to root
        run: |
          mkdir -p output_ready
          cp .github/output/*.txt output_ready/

      # 6. Push output_ready/*.txt to conf branch
      - name: Push to conf branch
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          git fetch origin conf || true
          git checkout -b temp-update

          # 清空 conf 分支根目录
          git rm -rf . || true
          git commit -m "cleanup" || true

          # 复制 output 文件到根目录
          cp output_ready/*.txt .

          git add *.txt
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto update routes - $(date -u +'%Y-%m-%d %H:%M:%S')"

          git push origin temp-update:conf --force

      # 7. Cleanup
      - name: Cleanup
        run: rm -rf output_ready .github/output/*
