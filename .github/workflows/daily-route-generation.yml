name: Daily CHN Route Generation

on:
  schedule:
    - cron: '30 16 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1. Checkout main
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # 2. Prepare working directory
      - name: Prepare working directory
        run: mkdir -p .github/temp/output

      # 3. Install Python
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - run: pip install requests
        working-directory: .github/temp

      # 4. Run Python script
      - name: Run generator
        run: python ../../gen-chnroute.py
        working-directory: .github/temp

      # 5. Copy output to root BEFORE checkout
      - name: Copy output to root
        run: |
          mkdir -p output_ready
          cp .github/temp/output/* output_ready/

      # 6. Push output_ready/* to conf branch
      - name: Push to conf branch
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          git fetch origin conf || true
          git checkout -b temp-update

          git rm -rf .   # 清空 temp-update 分支
          git commit -m "cleanup" || true

          cp output_ready/* .
          git add *.txt

          git diff --quiet && git diff --staged --quiet || git commit -m "Auto update routes - $(date -u +'%Y-%m-%d %H:%M:%S')"

          git push origin temp-update:conf --force

      # 7. Cleanup
      - name: Cleanup
        run: rm -rf output_ready .github/temp/output/*
